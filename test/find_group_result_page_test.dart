import 'package:flutter/material.dart';

import 'package:test/test.dart';

void main() {
  group('Join group', () {
    test('if user not in group group, members will increase by 1' , () {
      
    });
    test('if user already in group, group members will not increase' , () {
      
    });
    test('if group is full user doesent join group' , () {

    });
  });
}